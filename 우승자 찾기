#include <stdio.h>
int count[1001];
int cnt=0;
int winner[1001];
int main()
{
    int N;
    int K;
    scanf("%d",&N);
    scanf("%d",&K);
    for(int i=0;i<K;i++)
    {
        int player;
        scanf("%d",&player);
        count[player]++;
        if(count[player]==2)
        {
            bool con=false;
            for(int j=0;j<cnt;j++)
            {
                if(player==winner[j]) con=true;
            }
            if(con) continue;
            winner[cnt]=player;
            cnt++;
            for(int a=1;a<=N;a++) count[a]=0;
        }
    }
    printf("%d\n",cnt);
    for(int i=0;i<cnt;i++)
    {
        printf("%d ",winner[i]);
    }
    return 0;
}
